import React, { useEffect } from "react";
import { CircularProgress, Box, Grid, Typography, InputLabel, MenuItem, FormControl, Select } from "@mui/material";


const List = () => {

    return (
        <Box sx={{ padding: '25px' }}>
            <Typography variant="h4">
                Restaurants around you
            </Typography>
        </Box>
    )
}

export default List;